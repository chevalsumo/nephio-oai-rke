apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: local-path-provisioner
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Local Path Provisioner provides a way for the Kubernetes users to utilize the local storage in each node